/** @jsx jsx */
import { jsx } from '@emotion/core';
import 'twin.macro';

const Header = () => (
  <header tw="container mx-auto mt-8">
    <h1 tw="text-gray-900 text-4xl font-semibold tracking-wide">
      David Murdoch Graph Traversal
    </h1>
  </header>
);

export { Header };
